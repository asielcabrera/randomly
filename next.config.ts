import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    images: { unoptimized: true }, // quick fix; you can re-enable later
  reactStrictMode: true,
  /* config options here */
};

export default nextConfig;
